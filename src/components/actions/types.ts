export enum ActionTypes {
    fetchProducts, fetchDescription,
    addToCartAction, getItemAction, setMessage, clearMessage,
    restKey, fetchCartitems, removeFromcartAction, fetchCurrency, toggleBagAction, currencySymbolAction
}